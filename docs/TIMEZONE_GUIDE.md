# 🕐 Timezone Guide

## 🎯 Главный принцип

> **Время в системе = Греческое время (Europe/Athens)**
> 
> Независимо от того, где находится пользователь — в Австралии, США или Японии — 
> когда он вводит 14:00, это означает **14:00 по Греции**, потому что машина 
> находится в Греции и будет готова в 14:00 по местному (греческому) времени.

---

## ⚠️ Критически важно: НЕ использовать браузерную таймзону!

### Неправильный подход ❌

```javascript
// ❌ НЕПРАВИЛЬНО! Браузер в Австралии интерпретирует как австралийское время
const userTime = dayjs("14:00", "HH:mm");  // 14:00 AEST
const utcTime = userTime.utc();             // Конвертирует из AEST в UTC
// Результат: совершенно другое время!
```

### Правильный подход ✅

```javascript
// ✅ ПРАВИЛЬНО! Явно указываем, что это греческое время
const greekTime = dayjs.tz("14:00", "HH:mm", "Europe/Athens");  // 14:00 Athens
const utcTime = greekTime.utc();  // Конвертируем Athens → UTC
// Результат: правильное время относительно Греции
```

---

## Принцип: "Время как у авиабилетов"

### Концепция

Когда вы покупаете авиабилет из Нью-Йорка в Афины:
- **Вылет: 10:00** — это время Нью-Йорка (где самолёт)
- **Прилёт: 02:00+1** — это время Афин (где самолёт приземлится)

Аналогично в системе аренды автомобилей:
- **Получение: 10:00** — это ВСЕГДА время Греции (где машина)
- **Возврат: 18:00** — это ВСЕГДА время Греции (где машина)

**Независимо от того, где находится клиент, админ или суперадмин!**

### Пример: Пользователь из Австралии

```
Пользователь в Сиднее (AEST, UTC+11) бронирует машину на 14:00

Что он имеет в виду: 14:00 по Греции (когда он прилетит в Грецию)
Что мы сохраняем: 14:00 Athens = 12:00 UTC (зимой)

Если бы мы использовали его браузерную таймзону:
  14:00 AEST = 03:00 UTC ← НЕПРАВИЛЬНО! Это 3 ночи!
  
Используя греческую таймзону:
  14:00 Athens = 12:00 UTC ← ПРАВИЛЬНО!
```

---

## Текущая реализация

### Хранение в БД: UTC

```
База данных: MongoDB
Формат: ISO 8601 / JavaScript Date
Таймзона: UTC
```

### Пример

```
Клиент выбирает: 10:00 (Greece time)
↓
Система конвертирует: 10:00 Athens = 07:00 UTC (зимой) или 07:00 UTC (летом)
↓
В БД сохраняется: 2026-01-15T07:00:00.000Z
↓
При отображении: конвертируется обратно в Athens = 10:00
```

---

## ✅ Текущий код (ПРАВИЛЬНЫЙ)

### Сохранение времени

**AddOrderModal.js** (строки 355-366):
```javascript
// ✅ ПРАВИЛЬНО: Явно указываем Europe/Athens
const timeInLocal = dayjs.tz(
  `${bookDates.start} ${dayjs(startTime).format("HH:mm")}`,
  "YYYY-MM-DD HH:mm",
  "Europe/Athens"  // ← Браузерная таймзона игнорируется!
);

// Конвертируем в UTC для хранения
const timeInUTC = timeInLocal.utc().toDate();
```

**BookingModal.js** (строка 46 + использование):
```javascript
const TIME_ZONE = "Europe/Athens";  // Константа

// Использование (строки 424-434):
const pickupTime = dayjs.tz(
  `${startDateStr} ${dayjs(startTime).format("HH:mm")}`,
  "YYYY-MM-DD HH:mm",
  TIME_ZONE  // ← Греческое время!
);
```

### ⚠️ Отображение (ЕСТЬ ПРОБЛЕМЫ)

```javascript
// ❌ НЕПРАВИЛЬНО: показывает в браузерной таймзоне пользователя
dayjs(order.timeIn).format("HH:mm")

// ✅ ПРАВИЛЬНО: явно указываем греческую таймзону
dayjs(order.timeIn).tz("Europe/Athens").format("HH:mm")
```

### ✅ Чтение из БД (EditOrderModal.js)

```javascript
const timeZone = "Europe/Athens";  // строка 95

// При загрузке заказа (строки 252-259):
timeIn: dayjs.utc(order.timeIn).tz(timeZone),   // UTC → Athens
timeOut: dayjs.utc(order.timeOut).tz(timeZone), // UTC → Athens
```

### 📋 Итог: Текущая реализация ПРАВИЛЬНАЯ

| Операция | Код | Статус |
|----------|-----|--------|
| Сохранение (AddOrderModal) | `dayjs.tz(..., "Europe/Athens").utc()` | ✅ |
| Сохранение (BookingModal) | `dayjs.tz(..., TIME_ZONE).utc()` | ✅ |
| Чтение (EditOrderModal) | `dayjs.utc(order.timeIn).tz(timeZone)` | ✅ |
| Отображение (SuccessMessage) | `.tz("Europe/Athens").format()` | ✅ |

**Браузерная таймзона пользователя полностью игнорируется!**

---

## ⚠️ Проблемные места (требуют исправления)

### Статистика использования `dayjs().format()`:

| Файл | Всего `.format()` | С `.tz()` | Требует проверки |
|------|-------------------|-----------|------------------|
| EditOrderModal.js | 15 | 4 | ⚠️ 11 |
| Row.js | 33 | 0 | ⚠️ 33 (но это даты) |
| BookingModal.js | 10 | 2 | ⚠️ 8 |
| BigCalendar.js | 3 | 0 | ⚠️ 3 |
| OrdersByDateModal.js | 1 | 0 | ⚠️ 1 |
| Другие | ~40 | 0 | ⚠️ ~40 (в основном даты) |

### Критичные места (ВРЕМЯ):

Места где отображается `timeIn`/`timeOut` без `.tz("Europe/Athens")`:

```javascript
// ❌ НЕПРАВИЛЬНО — будет конвертироваться в таймзону браузера
dayjs(order.timeIn).format("HH:mm")

// ✅ ПРАВИЛЬНО — всегда показывает греческое время
dayjs(order.timeIn).tz("Europe/Athens").format("HH:mm")

// ✅ ИЛИ если данные из UTC
dayjs.utc(order.timeIn).tz("Europe/Athens").format("HH:mm")
```

### Менее критичные места (ДАТЫ):

Для дат (`YYYY-MM-DD`) проблема редко возникает, потому что:
- Даты хранятся как полночь UTC
- Сдвиг на день происходит только в крайних случаях (полночь + большой offset)

Но для надёжности рекомендуется везде использовать `.tz("Europe/Athens")`

---

## 🛠️ Решение: Централизованные утилиты

Создать файл `utils/businessTime.js` и использовать ВЕЗДЕ вместо прямых вызовов `dayjs().format()`:

```javascript
// utils/businessTime.js

import dayjs from "dayjs";
import utc from "dayjs/plugin/utc";
import timezone from "dayjs/plugin/timezone";

dayjs.extend(utc);
dayjs.extend(timezone);

export const BUSINESS_TZ = "Europe/Athens";

/**
 * Форматирует дату из БД для отображения пользователю
 * ВСЕГДА в греческой таймзоне, независимо от браузера
 */
export function formatDate(dbDate, format = "DD.MM.YYYY") {
  if (!dbDate) return "";
  return dayjs(dbDate).tz(BUSINESS_TZ).format(format);
}

/**
 * Форматирует время из БД для отображения пользователю
 * ВСЕГДА в греческой таймзоне, независимо от браузера
 */
export function formatTime(dbDate, format = "HH:mm") {
  if (!dbDate) return "";
  return dayjs(dbDate).tz(BUSINESS_TZ).format(format);
}

/**
 * Форматирует дату+время из БД
 */
export function formatDateTime(dbDate, format = "DD.MM.YYYY HH:mm") {
  if (!dbDate) return "";
  return dayjs(dbDate).tz(BUSINESS_TZ).format(format);
}

/**
 * Парсит введённое пользователем время как греческое
 * (НЕ использует браузерную таймзону!)
 */
export function parseBusinessTime(dateStr, timeStr) {
  return dayjs.tz(
    `${dateStr} ${timeStr}`,
    "YYYY-MM-DD HH:mm",
    BUSINESS_TZ
  );
}

/**
 * Конвертирует греческое время в UTC для сохранения в БД
 */
export function toStorageUTC(businessDayjs) {
  return businessDayjs.utc().toDate();
}

/**
 * Возвращает dayjs объект в греческой таймзоне из БД значения
 */
export function fromStorage(dbDate) {
  if (!dbDate) return null;
  return dayjs(dbDate).tz(BUSINESS_TZ);
}
```

### Использование:

```javascript
// ВМЕСТО:
dayjs(order.rentalStartDate).format("DD.MM.YY")  // ❌

// ИСПОЛЬЗУЕМ:
import { formatDate } from "@utils/businessTime";
formatDate(order.rentalStartDate, "DD.MM.YY")    // ✅


// ВМЕСТО:
dayjs(order.timeIn).format("HH:mm")              // ❌

// ИСПОЛЬЗУЕМ:
import { formatTime } from "@utils/businessTime";
formatTime(order.timeIn)                          // ✅
```

### Чек-лист рефакторинга:

- [ ] Создать `utils/businessTime.js`
- [ ] Заменить `dayjs(order.*).format()` на `formatDate/formatTime(order.*)`
- [ ] В первую очередь: файлы с отображением `timeIn`/`timeOut`
- [ ] Во вторую очередь: файлы с отображением дат

---

## Проблемы текущей реализации

| # | Проблема | Где встречается |
|---|----------|-----------------|
| 1 | Не везде конвертируется при отображении | `EditOrderModal`, `CalendarAdmin` |
| 2 | Разные способы парсинга времени | `BookingModal` vs `AddOrderModal` |
| 3 | Нет централизованного модуля | Код разбросан |
| 4 | DST (летнее время) не всегда учитывается | Переходные периоды |

---

## Рекомендуемое решение

### Вариант A: Сохранять в UTC (текущий подход, улучшенный)

**Плюсы:**
- Стандартный подход для БД
- Легко сортировать
- Совместимость с внешними системами

**Минусы:**
- Нужна конвертация при каждом отображении
- Риск ошибок при забытой конвертации

### Вариант B: Сохранять строкой с таймзоной

```javascript
// В БД сохраняем:
{
  timeIn: "2026-01-15T10:00:00+02:00",  // ISO 8601 с offset
  timeInTimezone: "Europe/Athens"
}
```

**Плюсы:**
- Всегда видно какое время имелось в виду
- Нет путаницы с конвертацией

**Минусы:**
- Сложнее сортировать
- Больше места в БД

### Вариант C: Сохранять только локальное время (рекомендуется для простоты)

```javascript
// В БД сохраняем:
{
  timeIn: "2026-01-15T10:00:00",  // Без timezone, всегда Athens
  _timezone: "Europe/Athens"      // Глобальная настройка компании
}
```

**Плюсы:**
- Максимальная простота
- Нет конвертации при отображении
- То, что сохранили = то, что показываем

**Минусы:**
- Если компания работает в нескольких часовых поясах — не подходит

---

## Реализация централизованного модуля

### Конфигурация

```javascript
// config/timezone.js

export const TIMEZONE_CONFIG = {
  // Бизнес-таймзона (где машины)
  BUSINESS_TIMEZONE: "Europe/Athens",
  
  // Формат хранения в БД
  STORAGE_FORMAT: "UTC",  // или "LOCAL"
  
  // Форматы отображения
  DISPLAY_FORMATS: {
    time: "HH:mm",
    date: "DD.MM.YYYY",
    datetime: "DD.MM.YYYY HH:mm",
    iso: "YYYY-MM-DDTHH:mm:ss",
  },
};
```

### Утилиты

```javascript
// utils/timezone.js

import dayjs from "dayjs";
import utc from "dayjs/plugin/utc";
import timezone from "dayjs/plugin/timezone";
import { TIMEZONE_CONFIG } from "@config/timezone";

dayjs.extend(utc);
dayjs.extend(timezone);

const { BUSINESS_TIMEZONE, STORAGE_FORMAT, DISPLAY_FORMATS } = TIMEZONE_CONFIG;

/**
 * Парсит введённое пользователем время как бизнес-время
 * @param {string} dateStr - "2026-01-15"
 * @param {string} timeStr - "10:00"
 * @returns {dayjs.Dayjs} - dayjs объект в бизнес-таймзоне
 */
export function parseInput(dateStr, timeStr) {
  return dayjs.tz(
    `${dateStr} ${timeStr}`,
    "YYYY-MM-DD HH:mm",
    BUSINESS_TIMEZONE
  );
}

/**
 * Подготавливает время для сохранения в БД
 */
export function toStorage(dayjsObj) {
  if (STORAGE_FORMAT === "UTC") {
    return dayjsObj.utc().toDate();
  }
  // Для LOCAL формата
  return dayjsObj.format("YYYY-MM-DDTHH:mm:ss");
}

/**
 * Читает время из БД и возвращает в бизнес-таймзоне
 */
export function fromStorage(dbValue) {
  if (!dbValue) return null;
  
  if (STORAGE_FORMAT === "UTC") {
    return dayjs(dbValue).tz(BUSINESS_TIMEZONE);
  }
  // Для LOCAL формата
  return dayjs.tz(dbValue, BUSINESS_TIMEZONE);
}

/**
 * Форматирует для отображения
 */
export function format(dbValue, formatType = "datetime") {
  const businessTime = fromStorage(dbValue);
  if (!businessTime) return "";
  return businessTime.format(DISPLAY_FORMATS[formatType] || formatType);
}

/**
 * Утилита для отладки
 */
export function debug(label, dbValue) {
  console.log(`[TIMEZONE DEBUG] ${label}:`);
  console.log(`  Raw value: ${dbValue}`);
  console.log(`  As UTC: ${dayjs(dbValue).utc().format()}`);
  console.log(`  As Athens: ${dayjs(dbValue).tz(BUSINESS_TIMEZONE).format()}`);
  console.log(`  Display: ${format(dbValue, "datetime")}`);
}
```

### React компоненты

```javascript
// components/common/Time.jsx

import React from "react";
import { format } from "@utils/timezone";
import { Tooltip } from "@mui/material";

/**
 * Компонент для безопасного отображения времени
 * Всегда показывает в бизнес-таймзоне
 */
export function Time({ value, type = "time" }) {
  if (!value) return <span>—</span>;
  
  const displayValue = format(value, type);
  const fullValue = format(value, "datetime");
  
  return (
    <Tooltip title={`${fullValue} (Greece)`}>
      <span className="time-display">{displayValue}</span>
    </Tooltip>
  );
}

/**
 * Время начала аренды
 */
export function PickupTime({ order }) {
  return <Time value={order.timeIn} type="time" />;
}

/**
 * Время окончания аренды
 */
export function ReturnTime({ order }) {
  return <Time value={order.timeOut} type="time" />;
}

/**
 * Полный диапазон аренды
 */
export function RentalPeriod({ order }) {
  return (
    <span className="rental-period">
      <Time value={order.rentalStartDate} type="date" />
      {" "}
      <Time value={order.timeIn} type="time" />
      {" → "}
      <Time value={order.rentalEndDate} type="date" />
      {" "}
      <Time value={order.timeOut} type="time" />
    </span>
  );
}
```

### Использование в формах

```javascript
// В AddOrderModal.js

import { parseInput, toStorage } from "@utils/timezone";

const handleBookingComplete = async () => {
  // 1. Парсим введённое время как греческое
  const timeInBusiness = parseInput(
    bookDates.start,
    dayjs(startTime).format("HH:mm")
  );
  
  const timeOutBusiness = parseInput(
    bookDates.end,
    dayjs(endTime).format("HH:mm")
  );
  
  // 2. Конвертируем для хранения
  const data = {
    timeIn: toStorage(timeInBusiness),
    timeOut: toStorage(timeOutBusiness),
    rentalStartDate: timeInBusiness.startOf("day").toDate(),
    rentalEndDate: timeOutBusiness.startOf("day").toDate(),
    // ...
  };
  
  // 3. Отправляем
  await addOrderNew(data);
};
```

### Использование при отображении

```javascript
// В EditOrderModal.js

import { Time, RentalPeriod } from "@components/common/Time";

// Вместо:
<Typography>
  {dayjs(order.timeIn).format("HH:mm")}  // ❌ Неправильно!
</Typography>

// Используем:
<Typography>
  <Time value={order.timeIn} type="time" />  // ✅ Всегда правильно
</Typography>

// Или для полного периода:
<RentalPeriod order={order} />
```

---

## Летнее время (DST)

### Греция

- **Летнее время:** Последнее воскресенье марта (02:00 → 03:00)
- **Зимнее время:** Последнее воскресенье октября (04:00 → 03:00)

### Как dayjs это обрабатывает

```javascript
// dayjs автоматически учитывает DST при использовании .tz()

// Зима (UTC+2)
dayjs.tz("2026-01-15 10:00", "Europe/Athens").utc().format()
// → "2026-01-15T08:00:00Z"

// Лето (UTC+3)  
dayjs.tz("2026-07-15 10:00", "Europe/Athens").utc().format()
// → "2026-07-15T07:00:00Z"
```

### Важно при переходе

В ночь перехода на летнее время час с 02:00 до 03:00 **не существует**.
В ночь перехода на зимнее время час с 03:00 до 04:00 **существует дважды**.

Рекомендация: Не создавать заказы с pickup/return в эти часы.

---

## Миграция существующих данных

Если в БД есть данные с неправильной таймзоной:

```javascript
// scripts/fix-timezone.js

import Order from "@models/order";
import dayjs from "dayjs";
import utc from "dayjs/plugin/utc";
import timezone from "dayjs/plugin/timezone";

dayjs.extend(utc);
dayjs.extend(timezone);

async function fixTimezone() {
  const orders = await Order.find({});
  
  for (const order of orders) {
    // Если timeIn сохранено неправильно (как локальное без конвертации)
    // Нужно понять как оно было сохранено и исправить
    
    // Пример: если 10:00 сохранено как 10:00 UTC вместо 10:00 Athens
    // То есть на 2-3 часа сдвинуто
    
    const originalTimeIn = dayjs(order.timeIn);
    
    // Интерпретируем как будто это было Athens время
    const correctedTimeIn = dayjs.tz(
      originalTimeIn.format("YYYY-MM-DDTHH:mm:ss"),
      "Europe/Athens"
    ).utc().toDate();
    
    order.timeIn = correctedTimeIn;
    // Аналогично для timeOut
    
    await order.save();
    console.log(`Fixed order ${order._id}`);
  }
}

fixTimezone();
```

---

## Чек-лист внедрения

### Шаг 1: Создать утилиты
- [ ] Создать `config/timezone.js`
- [ ] Создать `utils/timezone.js`
- [ ] Создать `components/common/Time.jsx`

### Шаг 2: Обновить формы создания
- [ ] `AddOrderModal.js` — использовать `parseInput`, `toStorage`
- [ ] `BookingModal.js` — использовать `parseInput`, `toStorage`
- [ ] `EditOrderModal.js` — использовать `parseInput`, `toStorage`

### Шаг 3: Обновить отображение
- [ ] Заменить все `dayjs(date).format()` на компонент `<Time />`
- [ ] Проверить `CalendarAdmin.js`
- [ ] Проверить `BigCalendar.js`
- [ ] Проверить `OrderCard.js` (если есть)

### Шаг 4: Тестирование
- [ ] Проверить создание заказа из разных таймзон
- [ ] Проверить отображение в разных таймзонах
- [ ] Проверить переход на летнее/зимнее время

### Шаг 5: Миграция (если нужно)
- [ ] Проанализировать текущие данные в БД
- [ ] Создать бэкап
- [ ] Запустить скрипт миграции
- [ ] Проверить результаты

---

## FAQ

### Q: Что если клиент в США выбирает 10:00?
**A:** Это означает 10:00 по Греции. Клиент должен понимать, что машина будет готова в 10:00 местного (греческого) времени.

### Q: Нужно ли показывать таймзону в UI?
**A:** Рекомендуется добавить маленькую пометку "(Greece time)" рядом с полями времени.

### Q: Что если в будущем будут машины в других странах?
**A:** Тогда нужно будет хранить таймзону для каждой компании/локации и использовать её вместо глобальной `BUSINESS_TIMEZONE`.

---

*Документация обновлена: Январь 2026*

